---
hosts: all
remote_user: username
become: yes
become_method: sudo
  
tasks:
- name: Remove keys for multiple users
  command: "rm -f /home/{{ item }}/.ssh/authorized_keys"
  with_items:
  - username1
  - username2
...